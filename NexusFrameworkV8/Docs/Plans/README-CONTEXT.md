# Planning reference, not current implementation status

These five phase plans were delivered before the source implementation candidate. Statements such as NOT_STARTED or documentation-only in these archived planning documents describe that earlier delivery.

Current implementation status is in `../PHASE-STATUS.md`; current test evidence is in `../VERIFICATION.md`. The root repository's `AGENT-INSTRUCTIONS.md` governs working-branch policy: use only `gpt6`, preserve other commits, and do not change `master` without authorization.

The source import does not approve any phase or waive missing implementation and native testing. Historical prototype links refer to the original documentation package or the corresponding files at the repository root.
